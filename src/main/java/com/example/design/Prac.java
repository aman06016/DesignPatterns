package com.example.design;

import java.util.concurrent.TimeUnit;

public class Prac {
    public static void main(String[] args) {
        long TIME_WINDOW = TimeUnit.MINUTES.toMillis(1);
        System.out.println(TIME_WINDOW);
    }
}
